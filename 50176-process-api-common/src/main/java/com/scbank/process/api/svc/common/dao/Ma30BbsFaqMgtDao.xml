<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scbank.process.api.svc.common.dao.Ma30BbsFaqMgtDao">

	<!-- Event 조회 [MA3_MAN.MA30_BBS_FAQ_MGT_S_01] -->
	<select id="selectFaq" parameterType="java.lang.String" resultType="com.scbank.process.api.svc.common.dao.dto.FaqResult">
		SELECT
			SEQNO
			, TITLE
			, CAST(W_DATE AS VARCHAR(10)) AS W_DATE
		FROM  MA30_BBS_FAQ_MGT
		WHERE LOCTN_CD=#{loctnCd}
		AND D_FLAG='Z00001'
		AND APP_STATUS = 'PF4005'
        AND UPPER(TITLE) LIKE UPPER('%'||#{searchWord}||'%')
        ORDER BY SEQNO DESC
        WITH UR
	</select>

</mapper>